void main() {
	char optstr[MAX_FLAGS * 2 + 2] =  {};

	char foo[] =
		"abc"
		"def";

	putchar('\n');
	!*foo()[3]++;

	12 * sizeof(int);

	242 * !(uint32_t) foo;

	1 + 2 * 3 + 4;
	a + b + c;
	a + b * c;
	(a + b) * c;
	a + b * !c;
	a + --c;
	pos.usec = next->pos_usec;
	pos.min = -1 + -1;

	if (!arr_pushi(a, i)) {
		fatal("failed");
	}

	putch(ch, &(c->out));
	sizeof(b->data);

	if((i+1)*(i+1) != *((int*)r)) {
		puts("");
	}
}
