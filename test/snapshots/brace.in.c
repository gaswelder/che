
int main() {
    if (t && (a || b)) {}

    void *diff = (char *)npp - (char *)pp;
    *(ep-1) = rune;

    if (!*argv || !*(argv + 1)) {
		return;
	}

    (++p)->inst = 0;

    a->len++;
}

uint32_t rotate(uint32_t value, int bits) {
	return (value << bits) | (value >> (32-bits));
}
